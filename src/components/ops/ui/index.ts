/**
 * The operations console's shared visual vocabulary, in one import.
 *
 *   import { OpsSection, OpsPanel, OpsStat } from '@/components/ops/ui';
 *
 * See ./primitives.tsx for what each one is for and when NOT to reach for a
 * raw className instead. The shell that these mount inside is
 * @/components/ops/OpsShell.
 */
export * from './primitives';
